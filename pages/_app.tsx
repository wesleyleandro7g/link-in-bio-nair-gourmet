import "../styles/globals.css";
import type { AppProps } from "next/app";

function LinkInBioNairGourmet({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default LinkInBioNairGourmet;
